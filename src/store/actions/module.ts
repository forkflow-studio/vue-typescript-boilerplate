import { ActionTree } from 'vuex'
import { ModuleState } from '@/types/module'
import RootState from '@/types/root-state'

const chatterActions: ActionTree<ModuleState, RootState> = {
}

export default chatterActions
