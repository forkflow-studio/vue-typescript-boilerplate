import { GetterTree } from 'vuex'
import { ModuleState } from '@/types/module'
import RootState from '@/types/root-state'

const moduleGetters: GetterTree<ModuleState, RootState> = {
}

export default moduleGetters
